import { GlobalThemeOverrides } from 'naive-ui'

export const darkThemeOverrides: GlobalThemeOverrides = {
  common: {
    // primaryColor: '#FFFFFF',
    // borderColor2: 'rgba(255, 255, 255, 0.04)'
  },
  Button: {
    // border: '1px solid rgba(255, 255, 255, 0.24)'
  }
}
