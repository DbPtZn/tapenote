import { PopoverTool, PopoverToolConfig } from '../../toolkit';
import { Injector } from '@textbus/core';

export declare function emojiToolConfigFactory(injector: Injector): PopoverToolConfig;
export declare function emojiTool(): PopoverTool;
