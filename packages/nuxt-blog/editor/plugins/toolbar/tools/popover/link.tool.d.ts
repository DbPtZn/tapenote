import { PopoverTool, PopoverToolConfig } from '../../toolkit';
import { Injector } from '@textbus/core';

export declare function linkToolConfigFactory(injector: Injector): PopoverToolConfig;
export declare function linkTool(): PopoverTool;
