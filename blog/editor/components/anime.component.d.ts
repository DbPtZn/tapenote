import { ComponentLoader } from '@textbus/platform-browser';
import { ComponentInitData, ComponentInstance, SlotRender, VElement } from '@textbus/core';

export declare const animeComponent: import('@textbus/core').Component<ComponentInstance<{
    render(slotRender: SlotRender): VElement;
}, any, any>, ComponentInitData<any, any>>;
export declare const animeComponentLoader: ComponentLoader;
