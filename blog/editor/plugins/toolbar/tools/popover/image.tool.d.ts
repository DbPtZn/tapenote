import { PopoverTool, PopoverToolConfig } from '../../toolkit';
import { Injector } from '@textbus/core';

export declare function imageToolConfigFactory(injector: Injector): PopoverToolConfig;
export declare function imageTool(): PopoverTool;
